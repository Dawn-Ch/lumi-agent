[project]
name = "minimal-swe-agent"
version = "0.1.0"
description = "A minimal SWE Agent built from scratch for learning purposes"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.91.0",
    "pydantic>=2.0",
    "python-dotenv>=1.1.1",
]

[project.scripts]
swe-agent = "main:main"
